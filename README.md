# 🥗 FitMeal AI — Telegram-бот для трекинга питания

Телеграм-бот для учёта КБЖУ с распознаванием еды по фото (Google Gemini), персональными целями, напоминаниями «пора поесть» и статистикой.

---

## Содержание

- [Функционал](#функционал)
- [API бота (команды и сценарии)](#api-бота-команды-и-сценарии)
- [Структура проекта](#структура-проекта)
- [База данных](#база-данных)
- [Установка и запуск](#установка-и-запуск)
- [Деплой](#деплой)

---

## Функционал

### 1. Профиль и цели КБЖУ

- **Онбординг (9 шагов):** вес → рост → возраст → пол → образ жизни → количество тренировок в неделю → тип тренировок → длительность → цель (похудеть / набрать / поддержать / сушка) → темп (медленно/быстро) → желаемый вес.
- **Расчёт целей ИИ (Gemini):** по формуле Миффлина–Сан Жеора считаются BMR и TDEE с учётом пола, образа жизни и тренировок. Для тренирующихся (3–4 раза в неделю) задаётся повышенный белок (1.8–2.2 г/кг), жиры не фиксируются шаблонными 28%, учитывается рекомпозиция при цели «поддержание» и целевом весе близком к текущему.
- **Коррекция BMR по полу:** после ответа модели BMR и TDEE пересчитываются в коде по полу, чтобы исключить ошибки формулы.
- **Норма воды:** считается по цели, темпу и суточным углеводам (формула: мл/кг по типу цели + +300 мл при углеводах &lt; 100 г). Сохраняется в профиле и показывается в ежедневных целях.
- **Ручная правка целей:** в профиле кнопка «Изменить цели КБЖУ» — по одной кнопке на калории, белки, жиры, углеводы; ввод одного числа с проверкой диапазона. После изменения пересчитывается норма воды.

### 2. Добавление еды

- **По фото:** отправка фото блюда или упаковки → Gemini возвращает название и КБЖУ (JSON) → подтверждение / исправить / отмена.
- **Текстом:** сообщение вида «гречка с курицей 300г» → расчёт КБЖУ через Gemini → подтверждение или уточнение (при `needs_clarification` бот может запросить уточнение и пересчитать).
- После подтверждения приём записывается в `meals` за текущую дату; при наличии целей показывается сводка за день и короткий совет (get_daily_tip).

### 3. Быстрое добавление

- Список пользовательских «быстрых» продуктов с сохранённым КБЖУ.
- Добавление нового: ввод названия и порции (как в текстовом добавлении) → сохранение в `quick_foods` и в список кнопок.
- Выбор продукта из списка → мгновенное добавление приёма за сегодня без повторного запроса к ИИ.
- Удаление быстрых продуктов через кнопку «Удалить» и выбор из списка.

### 4. «Что съесть?»

- Выбор приёма: завтрак / обед / ужин / перекус.
- ИИ даёт один конкретный вариант с учётом: целей на день, уже съеденного за сегодня (названия блюд), остатка по калориям и макросам. Для перекуса — только простые варианты (фрукты, творог, йогурт, орехи и т.п.), без сложных блюд вроде запечённого картофеля/батата. Ответ без markdown (звёздочки убираются при выводе).

### 5. Статистика

- **Где открыть:** блок «Статистика» открывается из **Мой профиль** по инлайн-кнопке «📊 Статистика» (в главном меню отдельной кнопки нет).
- **По умолчанию:** показывается сводка за **сегодня** (калории и макросы относительно целей) и кнопки выбора периода.
- **Неделя:** текст по дням (дата, калории, Б/Ж/У) за последние 7 дней.
- **Месяц:** то же за 30 дней + число дней с едой и средние калории в день.
- **Список веса:** текстовый список последних 30 записей веса (дата и вес в кг). Если задан целевой вес — показывается «До цели: X кг».

### 6. Вес

- **Записать вес:** ввод числа (кг) → запись в `weight_log` и обновление поля `weight` в профиле.
- **Пересчёт целей при смене веса:** после сохранения веса заново вызывается расчёт целей (ИИ или fallback-калькулятор) и пересчёт нормы воды; обновляются калории, белки, жиры, углеводы и вода в профиле. В ответе показываются новые цели и при наличии — расстояние до целевого веса.

### 7. Напоминания «пора поесть»

- Фоновая задача (каждые 15 минут) в окне **8:00–22:00** проверяет пользователей с включёнными напоминаниями и недобором по калориям/белку/углеводам (пороги: 50 ккал, 8 г белка, 15 г углеводов). Напоминание отправляется, когда прошло **не менее 45/90/120 минут** после последнего приёма (в зависимости от калорийности приёма) и не превышен лимит в день (2/3/4). Между двумя напоминаниями одному пользователю — не менее 90 минут.
- Формируется короткий совет (get_reminder_suggestion) с учётом времени суток и уже съеденного; отправка логируется в `reminder_log`.
- **Настройки в профиле:** кнопка «Напоминания «пора поесть»» — вкл/выкл и выбор количества напоминаний в день (2, 3 или 4).

### 8. Прочее

- **Сегодня:** список приёмов пищи за текущий день с КБЖУ по каждому и итогом по калориям/макросам (и по целям, если профиль заполнен). Кнопка «Удалить блюдо» открывает выбор блюда для удаления из дня.
- **Отмена последнего приёма:** команда `/undo` удаляет последнюю запись за сегодня из `meals`.

---

## API бота (команды и сценарии)

### Команды

| Команда | Описание |
|--------|----------|
| `/start` | Старт: если профиля нет — запуск онбординга (шаг 1/9); иначе — приветствие и сводка за сегодня. |
| `/setup` | Заново пройти все шаги профиля (вес → … → желаемый вес) с пересчётом целей. |
| `/settings` | То же, что кнопка «Мой профиль» — показать профиль и кнопки (цели КБЖУ, напоминания). |
| `/undo` | Удалить последний приём пищи за сегодня. |
| `/help` | Краткая справка по возможностям и командам. |

### Reply-кнопки (главное меню)

| Текст кнопки | Действие |
|--------------|----------|
| 📷 Добавить еду | Запрос фото или текстового описания еды. |
| 🍽 Сегодня | Список приёмов за сегодня + итог КБЖУ. |
| 💡 Что съесть? | Выбор приёма (завтрак/обед/ужин/перекус) → совет от ИИ. |
| ⚡ Быстрое добавление | Список быстрых продуктов или добавление нового. |
| 👤 Мой профиль | Показать профиль, цели, воду; кнопки «Изменить цели КБЖУ», «Напоминания», **«📊 Статистика»**. |
| 🏆 Результаты | Серии (белок, жиры, калории), рекорды и общая статистика по дням. |
| ⚖️ Записать вес | Запрос текущего веса → сохранение + пересчёт целей. |
| ❓ Что я умею | Краткая справка по возможностям и командам. |

### Callback_data (инлайн-кнопки)

**Еда (подтверждение):**

- `food_confirm` — принять КБЖУ и добавить приём.
- `food_edit` — перейти в режим исправления текста.
- `food_cancel` — отменить добавление.

**Приём пищи для совета:**

- `meal_breakfast`, `meal_lunch`, `meal_dinner`, `meal_snack` — выбор типа приёма для «Что съесть?».

**Статистика:**

- `stats_open` — открыть блок «Статистика» (из профиля): сводка за сегодня + кнопки периодов.
- `stats_week` — статистика по дням за 7 дней.
- `stats_month` — по дням за 30 дней + средние.
- `stats_weight` — список записей веса (текст, последние 30).

**Профиль:**

- `profile_edit_kbju` — экран ручной корректировки целей КБЖУ (4 кнопки полей).
- `profile_kbju_cal`, `profile_kbju_prot`, `profile_kbju_fat`, `profile_kbju_carb` — выбор поля, затем ввод одного числа.
- `profile_reminders` — экран настроек напоминаний.
- `profile_reminders_on`, `profile_reminders_off` — вкл/выкл.
- `profile_reminders_2`, `profile_reminders_3`, `profile_reminders_4` — количество напоминаний в день.

**Быстрое добавление:**

- `quick_add_{id}` — добавить быстрый продукт с данным `id` в приёмы за сегодня.
- `quick_new` — начать добавление нового быстрого продукта (ввод текста).
- `quick_delete` — выбор продукта на удаление.
- `quick_del_{id}` — удалить быстрый продукт с `id`.
- `quick_back` — вернуться к списку быстрых продуктов.

**Онбординг (профиль):** используются префиксы `gender_`, `lifestyle_`, `tc_`, `tt_`, `td_`, `goal_`, `pace_` с соответствующими значениями.

---

## Структура проекта

```
meal-fit-ai/
├── bot.py              # setup_bot_dp(), polling (python bot.py)
├── webhook_server.py   # Webhook для Render: aiohttp, set_webhook, POST /webhook
├── config.py           # BOT_TOKEN, GEMINI_API_KEY, DATABASE_URL из .env
├── database.py         # PostgreSQL (asyncpg): пул, init_db, users/meals/weight_log/reminder_log/quick_foods, все get/save (async)
├── calculator.py       # Миффлин–Сан Жеор, расчёт воды, format_daily_summary
├── gemini_helper.py    # Gemini: анализ фото/текста, расчёт целей, советы по приёму и напоминаниям
├── reminders.py        # run_reminders(bot), reminder_loop(bot) — по интервалу после последнего приёма, 8:00–22:00
├── keyboards.py        # main_keyboard, meal_choice_keyboard, confirm_food_keyboard, stats_keyboard, quick_foods_keyboard, gender_keyboard и др.
├── handlers/
│   ├── common.py       # /start, /help, Сегодня, Что съесть?, /undo
│   ├── food.py         # Добавить еду: фото, текст, подтверждение, исправление
│   ├── profile.py      # Мой профиль, онбординг, цели КБЖУ, напоминания, запись веса
│   ├── stats.py        # Статистика (сегодня/неделя/месяц/список веса), экран «Результаты»
│   └── quick.py        # Быстрое добавление: список, добавить, удалить
├── requirements.txt
├── Procfile            # web: python webhook_server.py
├── keep_alive.py       # Опционально: polling + HTTP для пингов (если не используешь webhook)
└── README.md
```

### Роль модулей

- **bot.py:** создаёт Bot и Dispatcher (`setup_bot_dp()`), подключает роутеры. Режим **polling** (`python bot.py`): снимает webhook, запускает `reminder_loop` и `start_polling`. Режим **webhook** (`python webhook_server.py`): ставит webhook, aiohttp принимает POST на `/webhook`.
- **database.py:** PostgreSQL (Neon) через asyncpg; при старте создаётся пул, вызывается `await init_db(pool)` (создание таблиц и при необходимости миграции колонок).
- **gemini_helper.py:** все запросы к Gemini (модель gemini-2.5-flash): анализ еды, расчёт целей, советы по приёму пищи и текст напоминания.
- **calculator.py:** локальный расчёт целей (fallback) и нормы воды; форматирование сводки за день.
- **reminders.py:** раз в 15 минут проверяет пользователей с недобором; напоминание отправляется, когда прошло достаточно времени после последнего приёма (45/90/120 мин) и не превышен лимит в день; пишет в `reminder_log`.

---

## База данных

**PostgreSQL (Neon)** через **asyncpg**. Строка подключения задаётся в переменной окружения `DATABASE_URL`; для Neon в URL автоматически добавляется `sslmode=require`, если его ещё нет.

### Таблица `users`

| Поле | Тип | Описание |
|------|-----|----------|
| user_id | BIGINT PK | Telegram user id |
| name | TEXT | Имя (опционально) |
| weight | REAL | Текущий вес, кг |
| height | REAL | Рост, см |
| age | INTEGER | Возраст |
| gender | TEXT | male / female |
| activity | TEXT | sedentary / light / active (образ жизни) |
| goal | TEXT | loss / gain / maintain / cutting / recomp или свой текст |
| target_weight | REAL | Желаемый вес, кг (NULL если не задан) |
| calories_goal | INTEGER | Цель по калориям в день |
| protein_goal | INTEGER | Цель по белкам, г |
| fat_goal | INTEGER | Цель по жирам, г |
| carbs_goal | INTEGER | Цель по углеводам, г |
| water_goal | INTEGER | Норма воды, мл |
| pace | TEXT | slow / fast |
| reminders_enabled | INTEGER | 1 — вкл, 0 — выкл |
| reminders_per_day | INTEGER | 2, 3 или 4 |
| created_at | TIMESTAMP | Время создания записи |

### Таблица `meals`

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL PK | Автоинкремент |
| user_id | BIGINT | Владелец |
| name | TEXT | Название блюда/продукта |
| calories | INTEGER | Ккал |
| protein | REAL | Белки, г |
| fat | REAL | Жиры, г |
| carbs | REAL | Углеводы, г |
| date | DATE | Дата |
| created_at | TIMESTAMP | Время добавления |

### Таблица `weight_log`

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL PK | Автоинкремент |
| user_id | BIGINT | Владелец |
| weight | REAL | Вес, кг |
| date | DATE | Дата записи |

### Таблица `reminder_log`

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL PK | Автоинкремент |
| user_id | BIGINT | Кому отправлено |
| sent_at | TIMESTAMP | Время отправки |
| date | DATE | Дата (для лимита в день) |

### Таблица `quick_foods`

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL PK | Автоинкремент |
| user_id | BIGINT | Владелец |
| name | TEXT | Название |
| calories | INTEGER | Ккал |
| protein | REAL | Белки, г |
| fat | REAL | Жиры, г |
| carbs | REAL | Углеводы, г |

---

## Установка и запуск

### 1. Клонирование и зависимости

```bash
git clone <repo-url>
cd meal-fit-ai
pip install -r requirements.txt
```

### 2. Переменные окружения

Создай файл `.env` в корне:

```
BOT_TOKEN=токен_от_BotFather
GEMINI_API_KEY=ключ_Google_AI_Studio
DATABASE_URL=postgresql://user:password@host/dbname
```

- **BOT_TOKEN** — обязателен (токен от [@BotFather](https://t.me/BotFather)).
- **DATABASE_URL** — обязателен; строка подключения к PostgreSQL (например [Neon](https://neon.tech)). Для Neon в URL автоматически добавляется `?sslmode=require`, если его ещё нет.
- **GEMINI_API_KEY** — без него не работают распознавание еды по фото/тексту, расчёт целей ИИ, советы «Что съесть?» и текст напоминаний (для целей используется fallback-калькулятор).

### 3. Запуск

```bash
python bot.py
```

Запускай только один экземпляр бота (один процесс с `getUpdates`). Иначе возможна ошибка `Conflict: terminated by other getUpdates request`.

---

## Деплой на Render (бесплатный тир) — Webhook

Бот работает через **webhook**: Telegram сам шлёт обновления на твой HTTPS-URL. Сервис просыпается, когда пользователь пишет боту — **UptimeRobot не нужен**.

### 1. Репозиторий на GitHub

Код в GitHub, например: [https://github.com/your-denys/meal-fit-ai](https://github.com/your-denys/meal-fit-ai)

### 2. Создание Web Service на Render

1. [render.com](https://render.com) → **Dashboard** → **New** → **Web Service**.
2. Подключи репозиторий **meal-fit-ai**.
3. Настрой:
   - **Name:** например `meal-fit-ai`.
   - **Region:** Frankfurt (или ближе).
   - **Branch:** `main`.
   - **Runtime:** Python 3 (в корне желательно `.python-version` с `3.12`).
   - **Build Command:** `pip install --upgrade pip && pip install -r requirements.txt`
   - **Start Command:** `python webhook_server.py`
4. **Environment** — добавь переменные:
   | Key | Value |
   |-----|--------|
   | `BOT_TOKEN` | токен от [@BotFather](https://t.me/BotFather) |
   | `DATABASE_URL` | строка подключения PostgreSQL ([Neon](https://neon.tech)) |
   | `WEBHOOK_BASE_URL` | **полный URL сервиса**, например `https://meal-fit-ai-xxxx.onrender.com` (без слэша в конце) |
   | `GEMINI_API_KEY` | ключ [Google AI Studio](https://aistudio.google.com/apikey) (опционально) |
   | `WEBHOOK_SECRET` | (опционально) секрет для заголовка `X-Telegram-Bot-Api-Secret-Token` |

   **Важно:** `WEBHOOK_BASE_URL` должен совпадать с URL сервиса в Render (указан в карточке сервиса). После первого деплоя скопируй URL и добавь в Environment, затем **Manual Deploy**.
5. **Create Web Service**. В логах будет: `Webhook установлен: https://.../webhook`.

### 3. Проверка

- Открой в браузере `https://твой-сервис.onrender.com` — ответ `FitMeal AI bot is alive!`.
- Напиши боту в Telegram `/start`. Первое сообщение после «сна» может прийти с задержкой 30–60 сек (cold start), дальше — сразу.

### Локальный запуск (polling)

Для разработки без webhook:

```bash
# В .env не задавай WEBHOOK_BASE_URL
python bot.py
```

### Важно

- Запускай только **один** инстанс бота с одним `BOT_TOKEN` (webhook или polling).
- Не запускай локально `python bot.py` с тем же токеном, что и на Render — обновления уйдут только в один из них.

---

*Документация актуальна для текущей версии кода.*
